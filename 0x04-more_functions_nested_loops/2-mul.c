#include "holberton.h"
/**
 * mul - multiplie two integers.
 * @a: first integer.
 * @b: second integer.
 * Return: always 0.
 */
int mul(int a, int b)
{
	int r = (a * b);

	return (r);
return (0);
}
