#include <stdio.h>


int factorial(int n)
{
	int result;
	int
	int  
/*	printf("El valor de n ahora es: %d\n",result);
*/
	if (n >= 1)
	{
	result = factorial (n * (ctr - 1));
	}
	ctr--;
	printf("El valor de n ahora es: %d\n",result);

		return(result);
}

int main(void)
{
    int r;

    r = factorial(5);
    printf("%d\n", r);
	return(0);
}
